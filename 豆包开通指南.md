# 🤖 豆包 API 开通指南(给 Deadline 用)

> 国内最省事的选择: 中文识别强、有免费额度、不用翻墙
> 全程 15-30 分钟, 需要**身份证实名**(学校会查)

---

## 1. 注册火山引擎账号(5 分钟)

打开 https://www.volcengine.com/

1. 点右上角 **注册**
2. 用**手机号**注册(国内手机就行)
3. 选**账号类型**:
   - 个人 / 学生: 选 **个人**
   - 公司: 不推荐(要营业执照)
4. 填完手机验证码 → 注册成功

---

## 2. 实名认证(5-10 分钟,最烦的一步)

登录后,系统会**强制要求实名**,否则开不了 API。

### 个人实名

1. 右上角头像 → **账号管理** → **实名认证**
2. 选 **个人认证**
3. 填:
   - 真实姓名
   - 身份证号
   - 上传身份证正反面照片
4. 等几秒到几分钟,**秒过**(不像银行那么严)

### ⚠️ 学校 / 特殊情况

- **学生证** = 身份证,直接用就行
- **没满 18 岁**:用父母的身份证(借一下拍照)
- **港澳台 / 外籍**:火山引擎有"海外用户"通道,但流程不一样,找客服

---

## 3. 开通"豆包大模型"(1 分钟)

1. 打开 https://www.volcengine.com/product/doubao
2. 点 **立即开通** / **免费体验**
3. 弹窗显示"开通成功"

**这一步不要钱**,只是开通了"使用权",还没真用。

---

## 4. 创建 API Key(2 分钟)

1. 打开 https://console.volcengine.com/ark
2. 左侧菜单 → **API Key 管理** / **在线推理**
3. 点 **创建 API Key**
4. 名字随便填,比如 `deadline-mvp`
5. **关键设置**:
   - 权限: 勾 **Doubao 大模型** 全套
   - 过期时间: 选 **永不过期**(或者自定义 1 年)
6. 点确定 → **会显示一串 key,只显示一次!**

```
示例: abcd-efgh-ijkl-mnop-1234-xxxx (格式大概这样)
```

**立刻复制保存到备忘录**,关掉就再也看不到了。丢了就再创一个。

---

## 5. 开通"在线推理" endpoint(关键!)

豆包跟 OpenAI 不一样,**不是直接传模型名**,要先创建一个"在线推理"。

1. 在 https://console.volcengine.com/ark 页面
2. 左侧 → **在线推理** → **自定义推理接入点**
3. 点 **创建接入点**:
   - 模型选择: **`doubao-1-5-vision-pro-32k-250115`** ← **Vision 模型**,能看图
     - 想要更便宜(便宜 30%):选 **`doubao-1-5-vision-lite-32k-250115`**
   - 名字: 随便,比如 `deadline-vision`
4. 创建后,**记下"接入点 ID"**或"模型名称",形如:
   ```
   ep-20240612xxxx-xxxx
   ```
   这个就是你要填到 Deadline "模型名" 里的东西

---

## 6. 充值 / 免费额度(5 分钟)

豆包有**新手免费额度**,但**不自动到账**,得手动领。

1. 打开 https://www.volcengine.com/activity/dashboard
2. 找 **"新人福利"** / **"免费试用"** banner
3. 点 **领取**
4. 一般送:
   - 50 万 tokens(够用 1-2 个月日常)
   - 部分 vision 模型有 1000 次免费图片识别

**实际够不够用**:
- 你每天传 10 张图 = 大约 2000 tokens
- 50 万 tokens = 够 250 天

### 实名后必须充值?

不必须。**有免费额度的情况下可以纯用免费额度**,0 成本。

但**超出免费额度会扣费**,要警惕。建议:
1. 第一次先**充值 1 元**(防止跑飞)
2. 设置**预算预警**: 火山引擎首页 → 费用 → 预警 → 设 5 元 / 10 元上限 → 邮件 / 短信通知

---

## 7. 填到 Deadline 里

打开 https://yangqunhub.github.io/deadline/ (或本地 `http://localhost:8765/`)

第一次会弹设置面板:

| 字段 | 填什么 |
|---|---|
| 🔗 **Base URL** | `https://ark.cn-beijing.volces.com/api/v3` |
| 🔑 **API Key** | 第 4 步拿到的 `abcd-efgh-...` |
| 🧩 **模型名** | 第 5 步拿到的 `ep-20240612xxxx-xxxx` (你的接入点 ID) |

填完点 **🧪 测试连接**,看到绿色 ✅ 就能用。

---

## 8. 常见问题(踩坑预警)

### ❌ "Failed to fetch" 

豆包**浏览器直调可能 CORS 拦**。备选:
1. 改用 OpenRouter(翻 [使用指南](./使用指南.md) 第 1 步)
2. 找作者要本地后端代理

### ❌ 测试连接 401 / 403

- API Key 错了 → 重新创建
- Key 权限没勾 Doubao 大模型 → 回第 4 步加权限
- 接入点 ID 拼错(注意大小写、`ep-` 前缀)

### ❌ 余额 / 额度 0

- 没领新人福利 → 回第 6 步
- 实名没完成 → 回到第 2 步

### ❌ "模型不存在"

- 接入点 ID 错(不是模型名!)
- 接入点没创建 / 删了

### ❌ 上传图片没反应

- 确认模型是 **vision** 版(有 `vision` 字样)
- 图片 > 5MB → 压一下再传

---

## 9. 不想自己搞?两条偷懒路径

### A. 借别人的 Key(不推荐但能跑)

- 找已经开通的朋友借 key 临时用
- 用完记得**让他在自己火山引擎后台删 key**

### B. 用 OpenRouter(更适合"我先试试"心态)

翻 [使用指南](./使用指南.md) 第 1 步,OpenRouter 注册 + 充值 $5 = 几十块 RMB = 够用大半年,**CORS 稳**。

---

## 10. 一句话总结

```
注册 → 实名 → 开通豆包 → 创 Key → 创接入点
                              ↓
                        填到 Deadline 设置里
                              ↓
                        测连接 → 用!
```

**第一次会卡 15-30 分钟,熟悉了 5 分钟搞定。**

> 💡 遇到问题别死磕,**把 F12 Console 报错截图发给我**,比文字描述快 10 倍。

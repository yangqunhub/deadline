# 📅 Deadline · 使用指南(给朋友看)

> 5 分钟跑起来,告别 deadline 漏通知
> 当前版本: **v2.1**(支持任务拆解 checklist)

---

## 1. 跑起来(3 步)

### 第 1 步:启动服务

打开终端(Windows 用 `cmd` / `PowerShell` / `Windows Terminal` 都行),输入:

```bash
cd "C:\Users\羊群的windows\Desktop\deadline"
python -m http.server 8765
```

**没装 Python?** 用这个替代(需要装 Node.js):

```bash
cd "C:\Users\羊群的windows\Desktop\deadline"
npx serve -p 8765
```

看到类似 `Serving HTTP on 0.0.0.0 port 8765` 就成功了。**这个窗口别关**,关了就停了。

### 第 2 步:打开浏览器

地址栏输入:

```
http://localhost:8765/
```

第一次打开会自动弹一个 **⚙️ 设置面板**,这是正常的。

### 第 3 步:填 API 配置(只填一次)

| 字段 | 填什么 | 备注 |
|---|---|---|
| 🔗 **Base URL** | 点下面 5 个预设链接一键填 | 推荐先试 🌐 OpenRouter(支持跨域最稳) |
| 🔑 **API Key** | 你的 key | **只存浏览器本地**,不会上传 |
| 🧩 **模型名** | 复制粘贴到输入框 | 常用 vision 模型见下表 |

**推荐 vision 模型**(以 OpenRouter 为例):

| 模型 | 适用 |
|---|---|
| `google/gemini-2.5-flash` | 便宜快,日常用 |
| `openai/gpt-4o-mini` | 中文识别好 |
| `anthropic/claude-3.5-sonnet` | 长文本/复杂截图 |
| `x-ai/grok-vision-beta` | 备用 |

填完**先点 🧪 测试连接**,看到绿色 ✅ 才能用。

> 💡 不知道用哪家?最省心是注册 [OpenRouter](https://openrouter.ai/),充值 $5 够用半年。

---

## 2. 怎么用(4 步闭环)

### ① 上传 / 粘贴

- **截图**:直接拖到虚线框,或点 "📷 点击选择"
- **纯文本**:直接粘贴到 "📝 或直接粘贴文本" 框
- **两者叠加**:截图 + 文字说明 = AI 识别更准

支持格式: JPG / PNG / WEBP,≤ 5MB
> ⚠️ **PDF 暂不支持**(v3 会加),有 PDF 需求先截图

### ② 选来源类型(让 AI 更准)

5 个 chip 任选:
- 💬 **QQ 群** · 📱 **微信** · 📄 **PDF/学习通** · 📝 **纯文本** · 🖼️ **其他**

### ③ 提取任务

点蓝色 **🤖 提取任务** 按钮,等几秒。

### ④ 确认 / 编辑 / 入库

每张卡显示 AI 识别出来的一条任务。**逐条检查**,重点看:
- 任务名对不对
- **截止时间**对不对(最关键!)
- 类型/重要度合不合理

---

## 3. 🧠 任务拆解(新功能!)

如果一个任务需要"分几步做"(比如期末论文、小组视频),点任务卡上的 **[🧠 智能拆解]** 按钮:

1. 弹窗里 AI 会问 1-3 个澄清问题(字数/类型/进度 等)
2. 一次答完所有问题,点 **🧠 生成拆解**
3. 卡片里出现 checklist,**每步可勾选**
4. 可手动增/删/改任意步
5. **全部勾完 = 任务自动标"已完成"**

例: 你截图传了"6/26 小组视频项目",AI 拆出 7 步:
- 找 3-4 名同学组队
- 共同填写群里共享文档
- 组内讨论确定选题
- 写 400-600 词视频稿
- 制作 PPT
- 录屏成 5 分钟视频
- 提交视频

---

## 4. 任务列表

顶部 tab 切换:

| Tab | 显示什么 |
|---|---|
| 📋 **待办** | 未完成的任务,**按截止时间升序**,红色 = 3 天内/过期 |
| ✅ **已完成** | 你点过 ✓ 的任务 |
| 🚫 **已取消** | 你点过 ✕ 的任务 |

视图切换(tab 右侧):
- 📋 列表 / 📅 周历(当前 7 天) / 🗓️ 月历(整月散点)

**搜索筛选**(列表视图才显示):搜索框 + 类型下拉 + 重要度下拉,**三个叠加生效**。

每条任务的操作:
- ✓ 完成 · ✕ 取消 · 🗑 删除 · 🔄 重新打开
- ✏️ **编辑**:改任务名/截止时间/checklist

---

## 5. 常见问题(踩坑预警)

### ❌ "Failed to fetch" 红色错误

**90% 是 CORS 跨域问题**。

- **用 OpenRouter / OpenAI**:已知支持,基本不踩
- **用豆包/通义国内服务**:浏览器直调可能拦。备用方案:
  1. 换 OpenRouter 试试
  2. 找我要一个最小后端代理(本地跑)

**打开 F12 → Console** 看具体错误,贴给我最快定位。

### ❌ AI 返回的不是任务

- 图太模糊 → 换张更清晰的
- 文字太小 → 放大截图重传
- 纯风景/无文字图 → AI 识别不出来正常
- 在 "📝 或直接粘贴文本" 里**加几句话描述**,AI 就能识别

### ❌ 任务突然没了

**清浏览器缓存 = localStorage 没了,正常**。

预防:
- 重要任务(期末/毕设)手动截图保存到相册
- 经常用的话,养成"截图后立即上传"的习惯

### ❌ 日期解析不对

- "下周五" → AI 会基于"今天"算
- "6/10" 没年 → 默认今年
- 不放心就在确认页**手动改截止时间**

### ❌ API 401 / 403 错误

- key 填错了
- key 过期了
- 模型名拼错了(注意大小写、横杠)

F12 → Network 标签能看到完整错误信息。

---

## 6. 小贴士

1. **每天打开看一眼**:顶部的 💡 提醒条不是装饰,真的有用
2. **截图+文字双输入**:QQ 群通知截图 + "我负责第 3 步" 这种文字说明,AI 提取准度 ↑
3. **一个任务一个截图**:多任务公告可以截多次,逐个传
4. **来源类型别选错**:选"QQ 群"AI 才知道是中文聊天语境
5. **checklist 准不准都告诉我**:哪怕是"AI 漏了关键一步",都是改进素材

---

## 7. 联系 / 反馈

- **bug / 建议**:直接微信找我
- **截图 + Console 报错**(F12 打开)反馈最快
- **想要的新功能**:在 README 路线图里挑

---

> 🚀 试完有想法,直接告诉我"xxx 不好用 / 想要 xxx"就行,比写需求文档管用。
